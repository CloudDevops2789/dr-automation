- name: DR Restore - Web + DB
  hosts: localhost
  connection: local
  gather_facts: no

  tasks:

    - name: Run restore role on target EC2
      include_role:
        name: app_restore
      vars:
        ansible_host: "{{ target_host }}"
        ansible_user: ubuntu
      delegate_to: "{{ target_host }}"
